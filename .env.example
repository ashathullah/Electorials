# Copy this file to .env and fill in values.
# PowerShell: Copy-Item .env.example .env

# === DEBUG MODE ===
# Enable debug logging and additional output
# DEBUG=1

# === AI CONFIGURATION ===
# Choose provider defaults (optional). Supported: gemini, openai
AI_PROVIDER=gemini

# Put your real key in .env (never commit it)
AI_API_KEY=YOUR_AI_API_KEY

# Gemini vision-capable model
AI_MODEL=gemini-2.5-flash

# Gemini OpenAI-compat base URL.
# You can set either of these; the script will normalize to /chat/completions.
AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/chat/completions

AI_TIMEOUT_SEC=120

# Optional (only set if your endpoint supports it)
# AI_RESPONSE_FORMAT=json_object

# --- Optional pricing (USD per 1M tokens) ---
# These values are written into the generated *-metadata.json under ai_metadata.pricing
# and used to compute ai_metadata.estimated_cost when token usage is provided by the API.
#
# Example (Groq: meta-llama/llama-4-maverick-17b-128e-instruct)
AI_INPUT_COST_PER_1M_USD=0.20
AI_OUTPUT_COST_PER_1M_USD=0.60
# AI_COST_CURRENCY=USD

# === AWS S3 CONFIGURATION ===
# For processing PDFs from S3 buckets
# AWS credentials (also supports IAM roles, AWS_PROFILE, etc.)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_SESSION_TOKEN=           # Optional: for temporary credentials

# AWS region (default: ap-south-1)
AWS_REGION=ap-south-1

# Default S3 bucket (optional)
# S3_DEFAULT_BUCKET=my-electoral-rolls

# Local directory for S3 downloads (uses temp if not set)
# S3_DOWNLOAD_DIR=./downloads

# Connection settings
# S3_CONNECT_TIMEOUT=10
# S3_READ_TIMEOUT=60
# S3_MAX_RETRIES=3
