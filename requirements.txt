# Electoral Roll PDF Processing - Dependencies
# Install: pip install -r requirements.txt

# PDF Processing
PyMuPDF>=1.24.0           # PDF to image extraction (import as fitz)

# Image Processing
opencv-python>=4.9.0      # Image manipulation, cropping, preprocessing
numpy>=1.26.0             # Array operations for image processing
Pillow>=10.2.0            # Image loading

# OCR - Tamil OCR (Primary - better accuracy for Tamil text)
ocr-tamil>=0.4.0          # Tamil OCR with CRAFT text detection
                          # https://github.com/gnana70/tamil_ocr
                          # Supports both Tamil and English text
                          # 10-40% faster than Tesseract/EasyOCR

# OCR - Tesseract (Optional fallback for EPIC/Serial numbers)
pytesseract>=0.3.10       # Tesseract OCR Python wrapper (optional)
# Note: Requires Tesseract OCR installed separately:
# - Windows: https://github.com/UB-Mannheim/tesseract/wiki
# - Linux: apt-get install tesseract-ocr tesseract-ocr-tam
# - macOS: brew install tesseract tesseract-lang

# AI Integration (for metadata extraction)
openai>=1.12.0            # OpenAI-compatible API client (works with Gemini too)
groq>=0.4.0               # Groq API client

# AWS S3 Integration (for remote file access)
boto3>=1.34.0             # AWS SDK for Python (S3 operations)

# Utilities
python-dotenv>=1.0.0      # Environment variable loading (optional, has built-in fallback)

# Development (optional)
# pytest>=8.0.0           # Testing
# black>=24.0.0           # Code formatting
# ruff>=0.2.0             # Linting
